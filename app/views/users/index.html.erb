<h1>All Users</h1>

<% @users.each do |user| %>
  <div>
    <%= user.profile_picture %>
  </div>
  <div>
    <%= user.full_name %>
  </div>

  <%= link_to "View Profiles", user_profile_path(user, user.profile) %>
  <hr>
<% end %>

<%= paginate @users %>
